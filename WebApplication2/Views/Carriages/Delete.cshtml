@model WebApplication2.Models.Carriage

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Carriage</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Train.LineNumber)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Train.LineNumber)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.CarriageNumber)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CarriageNumber)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
